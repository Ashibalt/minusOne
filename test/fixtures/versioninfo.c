/* Trusted fixture carrying version-information and manifest resources.
 * The binary itself is trivial; the point is its .rsrc directory. */
#include <stdio.h>

int main(void) {
    puts("versioninfo-fixture");
    return 0;
}
